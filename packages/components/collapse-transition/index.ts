import CollapseTransition from '../collapse/src/collapse-transition.vue';
import { withInstall } from '@db-plus/utils';

const DbCollapseTransition = withInstall(CollapseTransition);

export { DbCollapseTransition };

export default DbCollapseTransition;
